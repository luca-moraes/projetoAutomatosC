#include <stdio.h>

int main() {
    char word[10];
    int kQuantWords = 0;

    scanf("%s \n %d", word, &kQuantWords);

    printf("Word: %s\nNum: %d", word, kQuantWords);

    //printf("Hello, World!\n");
    return 0;
}
